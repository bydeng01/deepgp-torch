# benchmarks/

GPflux-vs-Torch parity harness and saved results. Not yet implemented.

Requires the optional `[parity]` extra (`pip install -e ".[parity]"`), which
installs GPflow/GPflux/TensorFlow. These are used only here and in the
integration golden-equivalence test — never imported by `src/deepgp`.
